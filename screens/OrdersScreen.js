import OrdersScreenOutput from "../components/UserComponents/Orders/OrdersScreenOutput";

function OrdersScreen({ navigation, route }) {
  return <OrdersScreenOutput navigation={navigation} route={route} />;
}

export default OrdersScreen;
